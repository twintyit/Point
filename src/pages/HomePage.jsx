import React from 'react';

const HomePage = () => {
    const email = localStorage.getItem('email');

    return (
        <div className="container">
            <h1>Goods will be coming soon...</h1>
        </div>
    );
};

export default HomePage;